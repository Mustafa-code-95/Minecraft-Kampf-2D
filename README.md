# README

Falls es Probleme bei der installation bitte installieren sie folgendes:

```shell
#!/bin/bash
sudo apt install python3
echo "python3 wurde installiert!"
sudo python3 -m venv /opt/python-env
sudo chmod -R 755 /opt/python-env
pip install panda3D
pip install numpy
pip install ursina
```
